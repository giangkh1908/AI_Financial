# ============================================================
# Dự án GURU · ViFinQA — dependencies
# M0: core. Retrieval deps (sentence-transformers, FlagEmbedding,
#     faiss-cpu, rank-bm25) sẽ thêm ở M3.
# ============================================================

# --- Core ETL / data ---
pandas>=2.0
numpy>=1.24
beautifulsoup4>=4.12
lxml>=4.9

# --- Config / settings ---
pyyaml>=6.0
pydantic>=2.5
pydantic-settings>=2.1

# --- LLM API (OpenRouter, OpenAI-compatible, vLLM) ---
openai>=1.30
httpx>=0.27

# --- Utils ---
tqdm>=4.66

# --- Dev / test ---
pytest>=8.0
