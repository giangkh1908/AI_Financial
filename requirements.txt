# ============================================================
# Dự án GURU · ViFinQA — dependencies
# M0: core. Retrieval deps (sentence-transformers, FlagEmbedding,
#     faiss-cpu, rank-bm25) sẽ thêm ở M3.
# ============================================================

# --- Core ETL / data ---
pandas>=2.0
numpy>=1.24
beautifulsoup4>=4.12
lxml>=4.9

# --- Config / settings ---
pyyaml>=6.0
pydantic>=2.5
pydantic-settings>=2.1

# --- LLM API (OpenRouter, OpenAI-compatible, vLLM) ---
openai>=1.30
httpx>=0.27

# --- Retrieval (M3) ---
# qdrant-client phải khớp version với server image (docker/docker-compose.yml).
# Mismatch → warning + có thể lỗi serialize. Pin cùng major.minor.
qdrant-client==1.19.0

# --- Rerank local (TÙY CHỌN — M3.1 đang TẮT, rerank.enabled=false) ---
# Chỉ cài khi bật rerank local (Qwen3-Reranker). Pipeline phục vụ không cần
# (lazy import trong pipeline._load_reranker).
# torch>=2.0
# transformers>=4.40

# --- Utils ---
tqdm>=4.66

# --- Dev / test ---
pytest>=8.0
